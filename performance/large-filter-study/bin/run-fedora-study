#!/usr/bin/env python3
"""Run native Fedora timing against an already installed server RPM."""

import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from study.run_study import main  # noqa: E402

raise SystemExit(main(forced_mode="native-timing"))

