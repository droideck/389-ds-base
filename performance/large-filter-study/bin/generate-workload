#!/usr/bin/env python3
"""Repository-local entry point for atomic workload generation."""

import sys

from pathlib import Path


STUDY_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(STUDY_ROOT))

from study.generator import main  # noqa: E402


if __name__ == "__main__":
    sys.exit(main())
